# Инструкция по деплою на Рег.ру

## 📋 Шаг 1: Добавление домена в панели управления

1. **Войдите в панель управления Рег.ру:**
   - Перейдите на https://www.reg.ru/user/
   - Войдите в аккаунт

2. **Перейдите в раздел "Хостинг":**
   - В меню выберите "Хостинг" → "Список сайтов"

3. **Добавьте домен:**
   - Нажмите кнопку "Добавить сайт" или "Подключить домен"
   - Введите ваш домен (например: `s0va.site` или `www.s0va.site`)
   - Выберите тип хостинга (обычно "Статический сайт" или "Веб-хостинг")

4. **Проверьте DNS-настройки:**
   - Убедитесь, что домен указывает на DNS-серверы Рег.ру:
     - `ns1.reg.ru`
     - `ns2.reg.ru`
   - Если DNS уже настроены, домен должен появиться в списке

---

## 📤 Шаг 2: Загрузка файлов сайта

### Вариант A: Через FTP

1. **Получите FTP-данные:**
   - В панели управления: "Хостинг" → "FTP-доступ"
   - Запишите:
     - FTP-сервер (обычно `ftp.ваш-домен.ru` или `ftp.reg.ru`)
     - Логин (обычно ваш домен)
     - Пароль

2. **Подключитесь через FTP-клиент:**
   - Используйте FileZilla, WinSCP или другой FTP-клиент
   - Подключитесь к серверу

3. **Загрузите файлы:**
   - Перейдите в папку `public_html` или `www` (корневая папка сайта)
   - Загрузите ВСЕ файлы из папки `frontend/`:
     - `index.html`
     - `about.html`
     - `services.html`
     - `gallery.html`
     - `contacts.html`
     - Папку `css/` со всем содержимым
     - Папку `images/` со всем содержимым

### Вариант B: Через файловый менеджер в панели управления

1. **Откройте файловый менеджер:**
   - В панели управления: "Хостинг" → "Файловый менеджер"
   - Выберите ваш домен

2. **Загрузите файлы:**
   - Перейдите в папку `public_html` или `www`
   - Используйте функцию "Загрузить файлы"
   - Загрузите все файлы из папки `frontend/`

### Вариант C: Через Git (если поддерживается)

1. **Проверьте поддержку Git:**
   - В панели управления: "Хостинг" → "Git"
   - Если доступно, настройте подключение к репозиторию:
     - URL: `https://github.com/EzGh0st67/SovaDetail.git`
     - Ветка: `main`
     - Папка деплоя: `public_html`

---

## 📁 Структура файлов на сервере

После загрузки структура должна быть такой:

```
public_html/
├── index.html
├── about.html
├── services.html
├── gallery.html
├── contacts.html
├── css/
│   ├── styles.css
│   └── components/
│       ├── header.css
│       └── footer.css
└── images/
    ├── favicon.ico
    └── ...
```

**Важно:** Все файлы должны быть в корневой папке `public_html` или `www`, а не в подпапке `frontend/`.

---

## ⚙️ Шаг 3: Настройка главной страницы

1. **Проверьте индексный файл:**
   - Убедитесь, что `index.html` находится в корневой папке
   - Это должна быть главная страница сайта

2. **Проверьте пути к файлам:**
   - В HTML файлах пути должны быть относительными:
     - `css/styles.css` (не `../css/styles.css`)
     - `images/favicon.ico` (не `../images/favicon.ico`)

---

## 🔍 Шаг 4: Проверка работы сайта

1. **Откройте сайт в браузере:**
   - Перейдите по адресу вашего домена
   - Например: `https://s0va.site` или `https://www.s0va.site`

2. **Проверьте все страницы:**
   - Главная (`/index.html` или `/`)
   - Услуги (`/services.html`)
   - Галерея (`/gallery.html`)
   - О нас (`/about.html`)
   - Контакты (`/contacts.html`)

3. **Проверьте ресурсы:**
   - CSS файлы загружаются
   - Изображения отображаются
   - Нет ошибок 404

---

## 🔧 Шаг 5: Настройка HTTPS (SSL-сертификат)

1. **Включите SSL:**
   - В панели управления: "Хостинг" → "SSL-сертификаты"
   - Активируйте бесплатный SSL-сертификат Let's Encrypt
   - Или используйте встроенный сертификат Рег.ру

2. **Настройте редирект на HTTPS:**
   - Обычно это делается автоматически
   - Или добавьте в `.htaccess` (если используется Apache):
   ```apache
   RewriteEngine On
   RewriteCond %{HTTPS} off
   RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
   ```

---

## ❗ Решение проблем

### Домен не отображается в панели

1. Проверьте, что DNS-серверы домена указывают на Рег.ру
2. Подождите 24-48 часов для распространения DNS
3. Обратитесь в поддержку Рег.ру

### Сайт не открывается

1. Проверьте, что файлы загружены в правильную папку (`public_html` или `www`)
2. Убедитесь, что `index.html` находится в корне
3. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### CSS/изображения не загружаются

1. Проверьте пути к файлам в HTML
2. Убедитесь, что все файлы загружены
3. Проверьте права доступа к папкам `css/` и `images/`

---

## 📞 Поддержка

Если возникли проблемы:
- Техподдержка Рег.ру: https://www.reg.ru/support/
- Чат поддержки в панели управления
- Телефон: указан в панели управления

---

**Успешного деплоя! 🚀**

